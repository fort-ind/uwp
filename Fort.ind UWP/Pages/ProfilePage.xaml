<Page
    x:Class="Fort.ind_UWP.ProfilePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Fort.ind_UWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Transparent">

    <Page.Resources>
        <Storyboard x:Key="AvatarFadeIn">
            <DoubleAnimation Storyboard.TargetName="AvatarGrid"
                             Storyboard.TargetProperty="Opacity"
                             From="0" To="1"
                             Duration="0:0:0.4"
                             EasingFunction="{StaticResource FadeEase}"/>
        </Storyboard>
        <CubicEase x:Key="FadeEase" EasingMode="EaseOut"/>
        <AcrylicBrush x:Key="CardAcrylicBrush"
                      BackgroundSource="Backdrop"
                      TintColor="{ThemeResource SystemChromeMediumHighColor}"
                      TintOpacity="0.6"
                      FallbackColor="{ThemeResource SystemChromeMediumHighColor}"/>
    </Page.Resources>

    <ScrollViewer HorizontalContentAlignment="Center">
        <StackPanel Padding="24" MaxWidth="600" HorizontalAlignment="Center">

            <!-- Not Logged In State -->
            <StackPanel x:Name="NotLoggedInPanel" Visibility="Collapsed">
                <Grid Background="{StaticResource CardAcrylicBrush}"
                      CornerRadius="4"
                      Padding="24">
                    <StackPanel HorizontalAlignment="Center" Spacing="16">
                        <FontIcon Glyph="&#xE77B;"
                                  FontSize="48"
                                  HorizontalAlignment="Center"/>
                        <TextBlock Text="Not signed in"
                                   FontSize="20"
                                   FontWeight="SemiBold"
                                   HorizontalAlignment="Center"/>
                        <TextBlock Text="Sign in with your fort.social account to see your profile here"
                                   TextWrapping="Wrap"
                                   TextAlignment="Center"
                                   Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
                        <Button x:Name="SignInButton"
                                Content="Sign In"
                                HorizontalAlignment="Center"
                                Click="SignInButton_Click"
                                Style="{StaticResource AccentButtonStyle}"/>
                    </StackPanel>
                </Grid>
            </StackPanel>

            <!-- Logged In State -->
            <StackPanel x:Name="LoggedInPanel" Spacing="16">

                <!-- Profile Header -->
                <Grid Background="{StaticResource CardAcrylicBrush}"
                      CornerRadius="4"
                      Padding="24">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Profile Picture -->
                    <Grid x:Name="AvatarGrid"
                          Width="80" Height="80"
                          CornerRadius="40"
                          Background="{ThemeResource SystemControlBackgroundBaseLowBrush}"
                          Opacity="0">
                        <Image x:Name="ProfileImage"
                               Stretch="UniformToFill"
                               Visibility="Collapsed"/>
                        <TextBlock x:Name="ProfileInitials"
                                   Text="?"
                                   FontSize="32"
                                   FontWeight="SemiBold"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"/>
                    </Grid>

                    <!-- Profile Info -->
                    <StackPanel Grid.Column="1" Margin="16,0,0,0" VerticalAlignment="Center">
                        <TextBlock x:Name="DisplayNameText"
                                   Text="Display Name"
                                   Style="{StaticResource SubtitleTextBlockStyle}"/>
                        <TextBlock x:Name="UsernameText"
                                   Text="@username@social.fort1nd.com"
                                   FontSize="14"
                                   Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
                        <TextBlock x:Name="MemberSinceText"
                                   Text="Member since..."
                                   FontSize="12"
                                   Foreground="{ThemeResource SystemControlForegroundBaseMediumLowBrush}"
                                   Margin="0,4,0,0"/>
                        <TextBlock x:Name="LastLoginText"
                                   Text=""
                                   FontSize="12"
                                   Foreground="{ThemeResource SystemControlForegroundBaseMediumLowBrush}"
                                   Margin="0,2,0,0"
                                   Visibility="Collapsed"/>
                    </StackPanel>
                </Grid>

                <!-- Bio Section -->
                <Grid Background="{StaticResource CardAcrylicBrush}"
                      CornerRadius="4"
                      Padding="20">
                    <StackPanel>
                        <TextBlock Text="About"
                                   FontSize="16"
                                   FontWeight="SemiBold"
                                   Margin="0,0,0,8"/>
                        <TextBlock x:Name="BioText"
                                   Text="No bio yet"
                                   TextWrapping="Wrap"
                                   Foreground="{ThemeResource SystemControlForegroundBaseMediumHighBrush}"/>
                    </StackPanel>
                </Grid>

                <TextBlock Text="Your profile is managed on fort.social. Edit your display name, bio, avatar, or account there."
                           TextWrapping="Wrap"
                           FontSize="12"
                           Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>

                <!-- Actions -->
                <StackPanel Orientation="Horizontal" Spacing="12">
                    <Button x:Name="ManageOnFortSocialButton"
                            Click="ManageOnFortSocialButton_Click"
                            AccessKey="M"
                            AutomationProperties.Name="Manage on fort.social button"
                            AutomationProperties.HelpText="Opens your fort.social profile in the browser (Alt+M)">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <FontIcon Glyph="&#xE774;" FontSize="14"/>
                            <TextBlock Text="Manage on fort.social"/>
                        </StackPanel>
                    </Button>
                    <Button x:Name="LogoutButton"
                            Click="LogoutButton_Click"
                            AccessKey="O"
                            AutomationProperties.Name="Sign out button"
                            AutomationProperties.HelpText="Sign out of your account (Alt+O)">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <FontIcon Glyph="&#xF3B1;" FontSize="14"/>
                            <TextBlock Text="Sign Out"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</Page>
