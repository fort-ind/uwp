<Page
    x:Class="Fort.ind_UWP.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Fort.ind_UWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Transparent">

    <Page.Resources>
        <!-- Acrylic background -->
        <AcrylicBrush x:Key="LoginAcrylicBrush"
                      BackgroundSource="HostBackdrop"
                      TintColor="{ThemeResource SystemChromeMediumLowColor}"
                      TintOpacity="0.7"
                      FallbackColor="{ThemeResource SystemChromeMediumLowColor}"/>

        <!-- Card background -->
        <AcrylicBrush x:Key="CardAcrylicBrush"
                      BackgroundSource="Backdrop"
                      TintColor="{ThemeResource SystemChromeMediumHighColor}"
                      TintOpacity="0.7"
                      FallbackColor="{ThemeResource SystemChromeMediumHighColor}"/>
    </Page.Resources>

    <Grid Background="{StaticResource LoginAcrylicBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="32"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title Bar -->
        <Grid x:Name="AppTitleBar" 
              Grid.Row="0" 
              Background="Transparent"
              HorizontalAlignment="Stretch">
            <TextBlock Text="Fort.ind"
                       VerticalAlignment="Center"
                       Margin="12,0,0,0"
                       FontSize="12"
                       Opacity="0.8"/>
        </Grid>

        <!-- Main Content -->
        <Grid Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Grid Background="{StaticResource CardAcrylicBrush}"
                  CornerRadius="12"
                  Padding="40"
                  MinWidth="380"
                  MaxWidth="420">
                
                <StackPanel Spacing="16">
                    <!-- Logo/Title -->
                    <StackPanel HorizontalAlignment="Center" Margin="0,0,0,16">
                        <TextBlock Text="??" 
                                   FontSize="48" 
                                   HorizontalAlignment="Center"/>
                        <TextBlock Text="Fort.ind" 
                                   FontSize="28" 
                                   FontWeight="Bold"
                                   HorizontalAlignment="Center"/>
                        <TextBlock Text="Sign in to your account" 
                                   FontSize="14"
                                   Opacity="0.7"
                                   HorizontalAlignment="Center"/>
                    </StackPanel>

                    <!-- Login Form -->
                    <StackPanel x:Name="LoginForm" Spacing="12">
                        <StackPanel>
                            <TextBox x:Name="UsernameBox"
                                     PlaceholderText="Username"
                                     FontSize="14"
                                     AutomationProperties.Name="Username"
                                     AutomationProperties.HelpText="Enter your username"/>
                            <TextBlock x:Name="UsernameHint"
                                       FontSize="11"
                                       Margin="0,2,0,0"
                                       Visibility="Collapsed"/>
                        </StackPanel>

                        <PasswordBox x:Name="PasswordBox"
                                     PlaceholderText="Password"
                                     FontSize="14"
                                     KeyDown="PasswordBox_KeyDown"
                                     AutomationProperties.Name="Password"
                                     AutomationProperties.HelpText="Enter your password"/>

                        <CheckBox x:Name="RememberMeCheck"
                                  Content="Remember me"
                                  IsChecked="True"
                                  AutomationProperties.Name="Remember me checkbox"/>

                        <TextBlock x:Name="ErrorText"
                                   Foreground="{ThemeResource SystemControlErrorTextForegroundBrush}"
                                   FontSize="12"
                                   TextWrapping="Wrap"
                                   Visibility="Collapsed"
                                   AutomationProperties.LiveSetting="Assertive"/>

                        <Button x:Name="LoginButton"
                                Content="Sign In"
                                HorizontalAlignment="Stretch"
                                Click="LoginButton_Click"
                                Style="{StaticResource AccentButtonStyle}"
                                AutomationProperties.Name="Sign in button"
                                AutomationProperties.HelpText="Press to sign in with your account"/>

                        <StackPanel Orientation="Horizontal" 
                                    HorizontalAlignment="Center"
                                    Spacing="4">
                            <TextBlock Text="Don't have an account?"
                                       Opacity="0.7"
                                       VerticalAlignment="Center"/>
                            <HyperlinkButton x:Name="ShowRegisterLink"
                                             Content="Create one"
                                             Click="ShowRegisterLink_Click"
                                             AutomationProperties.Name="Create account link"/>
                        </StackPanel>
                    </StackPanel>

                    <!-- Register Form (initially hidden) -->
                    <StackPanel x:Name="RegisterForm" Spacing="12" Visibility="Collapsed">
                        <StackPanel>
                            <TextBox x:Name="RegUsernameBox"
                                     PlaceholderText="Username"
                                     FontSize="14"
                                     TextChanged="RegUsernameBox_TextChanged"
                                     AutomationProperties.Name="Username"
                                     AutomationProperties.HelpText="Enter a username with only letters, numbers, hyphens and underscores"/>
                            <TextBlock x:Name="RegUsernameHint"
                                       FontSize="11"
                                       Margin="0,2,0,0"
                                       Visibility="Collapsed"/>
                        </StackPanel>

                        <TextBox x:Name="RegDisplayNameBox"
                                 PlaceholderText="Display Name (optional)"
                                 FontSize="14"
                                 AutomationProperties.Name="Display name"
                                 AutomationProperties.HelpText="Optional display name for your profile"/>

                        <StackPanel>
                            <TextBox x:Name="RegEmailBox"
                                     PlaceholderText="Email (optional)"
                                     FontSize="14"
                                     InputScope="EmailSmtpAddress"
                                     TextChanged="RegEmailBox_TextChanged"
                                     AutomationProperties.Name="Email address"
                                     AutomationProperties.HelpText="Optional email address for your account"/>
                            <TextBlock x:Name="RegEmailHint"
                                       FontSize="11"
                                       Margin="0,2,0,0"
                                       Visibility="Collapsed"/>
                        </StackPanel>

                        <PasswordBox x:Name="RegPasswordBox"
                                     PlaceholderText="Password"
                                     FontSize="14"
                                     PasswordChanged="RegPasswordBox_PasswordChanged"
                                     AutomationProperties.Name="Password"
                                     AutomationProperties.HelpText="Enter a password with at least 8 characters"/>
                        <TextBlock x:Name="RegPasswordStrengthText"
                                   FontSize="11"
                                   Margin="0,-4,0,0"
                                   Visibility="Collapsed"
                                   AutomationProperties.LiveSetting="Polite"/>

                        <PasswordBox x:Name="RegConfirmPasswordBox"
                                     PlaceholderText="Confirm Password"
                                     FontSize="14"
                                     KeyDown="RegConfirmPasswordBox_KeyDown"
                                     AutomationProperties.Name="Confirm password"
                                     AutomationProperties.HelpText="Re-enter your password to confirm"/>

                        <TextBlock x:Name="RegErrorText"
                                   Foreground="{ThemeResource SystemControlErrorTextForegroundBrush}"
                                   FontSize="12"
                                   TextWrapping="Wrap"
                                   Visibility="Collapsed"
                                   AutomationProperties.LiveSetting="Assertive"/>

                        <Button x:Name="RegisterButton"
                                Content="Create Account"
                                HorizontalAlignment="Stretch"
                                Click="RegisterButton_Click"
                                Style="{StaticResource AccentButtonStyle}"
                                AutomationProperties.Name="Create account button"
                                AutomationProperties.HelpText="Press to create your new account"/>

                        <StackPanel Orientation="Horizontal" 
                                    HorizontalAlignment="Center"
                                    Spacing="4">
                            <TextBlock Text="Already have an account?"
                                       Opacity="0.7"
                                       VerticalAlignment="Center"/>
                            <HyperlinkButton x:Name="ShowLoginLink"
                                             Content="Sign in"
                                             Click="ShowLoginLink_Click"
                                             AutomationProperties.Name="Sign in link"/>
                        </StackPanel>
                    </StackPanel>

                    <!-- Skip for now option -->
                    <HyperlinkButton x:Name="SkipButton"
                                     Content="Continue without account"
                                     HorizontalAlignment="Center"
                                     Opacity="0.6"
                                     Click="SkipButton_Click"
                                     AutomationProperties.Name="Skip sign in"
                                     AutomationProperties.HelpText="Continue using the app without signing in"/>
                </StackPanel>
            </Grid>
        </Grid>

        <!-- Loading overlay -->
        <Grid x:Name="LoadingOverlay" 
              Grid.RowSpan="2" 
              Background="{ThemeResource SystemControlPageBackgroundMediumAltMediumBrush}" 
              Visibility="Collapsed">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressRing IsActive="True" Width="40" Height="40"/>
                <TextBlock Text="Please wait..." Margin="0,12,0,0" Opacity="0.8"/>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
