<Page
    x:Class="Fort.ind_UWP.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Fort.ind_UWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d"
    Background="Transparent"
    Loaded="LoginPage_Loaded">

    <!-- No background of its own. This page is hosted in MainPage's ContentFrame, over RootGrid's
         acrylic, which carries the user's chosen tint; painting AppSurfaceAcrylicBrush here laid the
         untinted default over it, so the tint vanished for as long as Login was showing. The
         32px title-bar row that used to sit above the card is gone for the same reason: it dates
         from when this was a top-level page, and MainPage already owns the title bar. -->
    <Grid Background="Transparent">
        <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
            <Grid Background="{ThemeResource CardBackgroundBrush}"
                  BorderBrush="{ThemeResource CardBorderBrush}"
                  BorderThickness="1"
                  Padding="40"
                  MinWidth="380"
                  MaxWidth="420">

                <StackPanel Spacing="16">
                    <StackPanel HorizontalAlignment="Center" Margin="0,0,0,16">
                        <FontIcon Glyph="&#xE72E;"
                                  FontSize="48"
                                  HorizontalAlignment="Center"/>
                        <TextBlock x:Uid="LoginFortDesktop"
                                   Style="{StaticResource TitleTextBlockStyle}"
                                   HorizontalAlignment="Center"/>
                        <TextBlock x:Uid="LoginSignInWithYour"
                                   Style="{StaticResource BodyTextBlockStyle}"
                                   Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                                   HorizontalAlignment="Center"
                                   TextWrapping="Wrap"
                                   TextAlignment="Center"/>
                    </StackPanel>

                    <TextBlock x:Uid="LoginYouLlBeTaken"
                               Style="{StaticResource CaptionTextBlockStyle}"
                               Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                               TextWrapping="Wrap"
                               TextAlignment="Center"/>

                    <TextBlock x:Name="ErrorText"
                               Foreground="{ThemeResource SystemControlErrorTextForegroundBrush}"
                               Style="{StaticResource CaptionTextBlockStyle}"
                               TextWrapping="Wrap"
                               TextAlignment="Center"
                               Visibility="Collapsed"
                               AutomationProperties.LiveSetting="Assertive"/>

                    <Button x:Uid="SignInButton" x:Name="SignInButton"
                            HorizontalAlignment="Stretch"
                            Click="SignInButton_Click"
                            Style="{StaticResource AccentButtonStyle}">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Spacing="8">
                            <FontIcon Glyph="&#xE774;" FontSize="14"/>
                            <TextBlock x:Uid="LoginSignInWithFort"/>
                        </StackPanel>
                    </Button>

                    <HyperlinkButton x:Uid="SkipButton" x:Name="SkipButton"
                                     HorizontalAlignment="Center"
                                     Click="SkipButton_Click"/>

                    <muxc:TeachingTip x:Uid="SkipHintTip" x:Name="SkipHintTip"
                                      Target="{x:Bind SkipButton}"
                                      PreferredPlacement="Bottom"/>
                </StackPanel>
            </Grid>
        </Grid>

        <Grid x:Name="LoadingOverlay"
              Background="{ThemeResource SystemControlPageBackgroundMediumAltMediumBrush}"
              Visibility="Collapsed">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="8">
                <ProgressRing x:Uid="LoginWaitingForSignIn" IsActive="True" Width="40" Height="40" HorizontalAlignment="Center"/>
                <TextBlock x:Uid="LoginWaitingForFortSocial" Margin="0,12,0,0"
                           Foreground="{ThemeResource SystemControlForegroundBaseMediumHighBrush}"
                           TextAlignment="Center"
                           AutomationProperties.LiveSetting="Polite"/>
                <HyperlinkButton x:Uid="CancelSignInButton" x:Name="CancelSignInButton"
                                 HorizontalAlignment="Center"
                                 Click="CancelSignInButton_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
